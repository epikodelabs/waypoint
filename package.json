{
  "name": "waypoint",
  "version": "1.0.2",
  "scripts": {
    "ng": "ng",
    "start": "ng serve app1",
    "build": "ng build waypoint",
    "watch": "ng build waypoint --watch --configuration development",
    "test": "testify --headless --browser chrome",
    "test:watch": "npx testify",
    "test:node": "npx testify --browser node",
    "test:coverage": "npx testify --headless --browser chrome --coverage",
    "start:server": "npm run build:builder && ng serve app2-server",
    "build:server": "npm run build:builder && ng build app2-server",
    "start:client": "npm run build:builder && ng serve app2-client",
    "build:client": "npm run build:builder && ng build app2-client --configuration development",
    "watch:client": "npm run build:builder && ng build app2-client --watch --configuration development",
    "verify": "npm run build:builder && npm run build && ng build app1 --configuration development && ng build app2-client --configuration development && ng build app2-server --configuration development && npm test",
    "build:builder": "node projects/tools/builder/scripts/build.mjs",
    "clean:builder": "node projects/tools/builder/scripts/clean.mjs"
  },
  "private": true,
  "type": "module",
  "packageManager": "npm@12.0.2",
  "dependencies": {
    "@angular/common": "22.1.0",
    "@angular/compiler": "22.1.0",
    "@angular/core": "22.1.0",
    "@angular/forms": "22.1.0",
    "@angular/platform-browser": "22.1.0",
    "@angular/platform-server": "22.1.0",
    "@angular/router": "22.1.0",
    "@angular/ssr": "22.1.2",
    "express": "5.1.0",
    "rxjs": "7.8.2",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@angular/build": "22.1.2",
    "@angular/cli": "22.1.2",
    "@angular/compiler-cli": "22.1.0",
    "@epikodelabs/testify": "^1.0.51",
    "@types/express": "5.0.3",
    "@types/jasmine": "^6.0.0",
    "@types/node": "24.3.0",
    "ng-packagr": "22.1.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "esbuild": "^0.28.2",
    "@epikodelabs/waypoint-builder": "file:projects/tools/builder"
  }
}