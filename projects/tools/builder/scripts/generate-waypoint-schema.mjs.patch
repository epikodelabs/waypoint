/*
Remove `routesExport` from the normal generated Waypoint schema.

If compatibility is required, keep it under an advanced/deprecated option:

waypoint: {
  properties: {
    entry: ...,
    profile: ...,
    buildManifest: ...,
    rootExport: {
      type: 'string',
      deprecated: true,
      description:
        'Compatibility override for selecting one root tree export. Normally unnecessary because the entry module export graph is collected automatically.'
    }
  }
}
*/
