{
  "name": "@epikodelabs/route-compiler",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Route compiler scaffold for protected route artifacts.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  }
}

