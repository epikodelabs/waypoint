{
  "name": "@epikodelabs/route-compiler",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Waypoint server-side routing compiler for authorization metadata and isolated browser artifacts.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test ../../../dist/tools/compiler/tests/*.spec.js"
  }
}
