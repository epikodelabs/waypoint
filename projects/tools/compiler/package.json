{
  "name": "@epikodelabs/route-compiler",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Build-time compiler for Waypoint server-side routing, isolated route artifacts, and server authorization metadata.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node --test ../../../dist/tools/compiler/tests/*.spec.js"
  }
}
